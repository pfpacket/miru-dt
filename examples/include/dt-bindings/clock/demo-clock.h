/* Minimal demo clock bindings header, included by both soc.dtsi and
 * board.dts so the dependency graph shows a shared include. */
#ifndef _DT_BINDINGS_CLOCK_DEMO_CLOCK_H
#define _DT_BINDINGS_CLOCK_DEMO_CLOCK_H

#define DEMO_CLK_CPU 0
#define DEMO_CLK_UART 1

#endif
